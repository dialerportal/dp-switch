# SIP username probing seen at the portal's directory endpoint. A source asking
# for users that do not exist is enumerating extensions, not misconfigured.
[Definition]
failregex = sip directory (?:miss|rejected)[^\n]*from <HOST>
ignoreregex =
datepattern = ^\[%%Y-%%m-%%d %%H:%%M:%%S\]
